import '@testing-library/jest-dom';

// Stub window.matchMedia — not implemented in jsdom but required by
// Radix UI primitives used in the component tree.
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
  }),
});
